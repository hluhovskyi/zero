name: CI

on:
  pull_request:
    branches: [ master ]

concurrency:
  group: ci-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-22.04-arm

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: changes
        name: Detect runtime changes
        run: |
          if git diff --name-only origin/${{ github.base_ref }}...HEAD \
              | grep -qvE '(\.md$|^(skills|docs|\.claude)/)'; then
            echo "runtime=true" >> "$GITHUB_OUTPUT"
            echo "Runtime files changed — running gradle."
          else
            echo "Only non-runtime files changed — skipping gradle."
          fi

      - if: steps.changes.outputs.runtime == 'true'
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - if: steps.changes.outputs.runtime == 'true'
        uses: gradle/actions/setup-gradle@v3

      - if: steps.changes.outputs.runtime == 'true'
        name: Run spotless, tests, lint
        run: ./gradlew spotlessCheck testAll lintDebug

      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: '**/build/reports/'
